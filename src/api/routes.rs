// Re-export handlers for convenience
pub use crate::api::handlers::*;
pub use crate::api::websocket::websocket_handler;

// This module could contain additional route-specific logic
// For now, all routes are defined in the main API module